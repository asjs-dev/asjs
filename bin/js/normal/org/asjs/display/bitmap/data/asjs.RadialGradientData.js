ASJS.RadialGradientData = function( x0, y0, r0, x1, y1, r1 ) {
	var that = {};
		that.x0 = x0 || 0;
		that.y0 = y0 || 0;
		that.r0 = r0 || 0;
		that.x1 = x1 || 0;
		that.y1 = y1 || 0;
		that.r1 = r1 || 0;
	return that;
}
